<%= form_with(model: train) do |form| %>
  <% if train.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
        <% train.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :train_name, style: "display: block" %>
    <%= form.text_field :train_name %>
  </div>

  <div>
    <%= form.label :train_capacity, style: "display: block" %>
    <%= form.number_field :train_capacity %>
  </div>

  <div>
    <%= form.number_field :user_id, id: :train_user_id, value:current_user.id,type: :hidden  %>
  </div>

  <div>
    <%= form.submit "Add Train",class:"btn btn-app mt-3" %>
  </div>
<% end %>
